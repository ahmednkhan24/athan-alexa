/* eslint-disable no-unused-vars */
const {
  createMomentObject,
  createMomentTime,
  subtractAndFormat
} = require('../src/dateAndTime');

describe('dateAndTime', () => {
  it('tests', () => {
    expect(1).toEqual(1);
  });
});
